# ---------------------------------------------------------------------------
# Runtime dependencies (pinned for reproducible Docker builds).
# The old file listed the developer's whole machine (PyQt5, spyder, black...);
# only server-side packages are kept here.
# ---------------------------------------------------------------------------

# Core
Django==5.0.14
djangorestframework==3.15.2
python-decouple==3.8

# Auth / social login (Google)
django-allauth==0.63.6
PyJWT==2.9.0
cryptography==43.0.3
requests==2.32.3
requests-oauthlib==2.0.0
oauthlib==3.2.2

# ASGI / realtime (chat)
channels==4.1.0
daphne==4.1.2

# Frontend helpers
django-htmx==1.19.0
markdown2==2.5.1

# Media / files
Pillow==10.4.0
python-slugify==8.0.4
openpyxl==3.1.5

# LLM client used by chat consumers (optional feature)
ollama==0.3.3

# Database (PostgreSQL)
psycopg[binary]==3.2.3

# Static files + WSGI server
whitenoise==6.7.0
gunicorn==22.0.0
